# infra

Infrastructure notes, deployment templates, and environment-specific configuration live here.

Current production web deployment target:

- Cloudflare Pages project rooted at `apps/web`
- Build command: `npm run build`
- Build output directory: `dist`
